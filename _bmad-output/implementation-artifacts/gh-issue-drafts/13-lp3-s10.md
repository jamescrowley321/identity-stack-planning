<!-- DRAFT GH ISSUE — local review only, DO NOT FILE until owner approves.
Target repo: jamescrowley321/py-identity-model
Suggested labels: load-testing, docs, tech-debt
-->
# Title
LP-3 (TH-1.5 leftover): resolve S10 — de-scope the blocking-validator scaffold honestly

# Body
S10 (`scenarios.py:355-367`, "blocking claims_validator — SCAFFOLD") is an explicit placeholder: DIAGNOSTIC-only, drives no assertion, "Do NOT treat as coverage." So "S1–S12 coverage" is really 11 scenarios. Make the coverage claim honest.

## Change
- Formally **de-scope** S10 from the S1–S12 coverage matrix: relabel it "deferred backlog — needs a blocking custom-validator wired into the RS app (a separate feature)."
- Reflect in the load `README.md` coverage table (S10 = deferred, not covered).
- Keep the row as a tracked backlog marker (do not silently delete) so the future feature has a home.

## Definition of done
- No doc or matrix presents S10 as delivered coverage.
- A tracked backlog note names what implementing it requires.

## Files
`src/tests/load/scenarios.py`, `src/tests/load/README.md`. Branch: off LP-2.
